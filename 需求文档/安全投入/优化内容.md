## 安全费用标准库列表

1. 行业分类树去掉顶部的行业分类标题
2. 新建按钮行的右侧不需要显示当前选中的内容
3. 查询、重置按钮默认居右放置
4. 编码不需要蓝色字体和点击跳转的交互，应当设置给提取类型字段，并且超链接文本默认为蓝色下划线的文字，点击后跳转到详情页
5. 提取周期类型和提取规则类型不要用标签的样式，用文字展示即可
6. 操作列按钮为纯图标按钮，不需要边框底色，不需要按钮文字

## 安全费用标准库详情页

1. 不要顶部的页面名称行
2. 详细提取规则行的三个字段均为必填项，增加必填项的*
3. 投入范围和单位配置的表格上方不需要显示投入类别列表和关联组织列表文字
4. 投入范围和单位配置的添加、删除按钮居左显示

## 公司投入预算列表页

1. 不要操作按钮区上方的公司投入预算标题行
2. 不需要显示操作按钮区右侧的共x条记录的文字
3. 查询、重置按钮默认居右放置
4. 编号不需要蓝色字体和点击跳转的交互，应当设置给投入预算（元）字段，并且超链接文本默认为蓝色下划线的文字，点击后跳转到详情页
5. 操作列按钮为纯图标按钮，不需要边框底色，不需要按钮文字

## 公司投入预算详情页

1. 不要顶部的页面名称行
2. 提取依据值（元）、应提取金额（元）、上一周期结余金额（元）、投入预算（元）均为必填项，增加必填项的*
3. 提取依据值（元）、应提取金额（元）、投入预算（元）字段下方去掉小字提示
4. 应提取金额（元）用文本输入框的方式展示数据，为不可编辑状态
5. 投入预算（元）用文本输入框的方式展示数据，可编辑

## 数据看板

1. 同比和环比的数值看起来非常的不显眼，尤其是和卡片背景为同样的色调时，请调整设计
2. 年份选择器放到2026年度安全投入标题文字的右侧
3. 调整数据看板的页面，希望可以一页放下，不需要滚动查看

## 统计分析

1. 部门树不需要标题
2. 查询区域不需要显示当前选择的部门节点
3. 卡片区域小一些，合理调整一下高度



## 安全费用计提管理详情页

1. 提取依据值字段的输入组件右侧不需要“万元”字样
2. 本周期应提取金额（万元）的输入组件右侧增加一个外键图标按钮，点击图标按钮可以打开弹窗查看计算规则，将计算规则以可视查看的方式显示出来
3. 本页面内关于金额的字段或者列表内的数值，如果是小数点末尾的0请不显示便于查看数据
4. 本页面内每个字段的字段名称请设置最多显示6个字，如果超长了请换行显示
5. 本周期计提金额（万元）字段改为可以编辑
6. 详细计划的合计以表格行的形式展示在最下面一行

## 20260108-001优化内容

### 计提标准库

1. 详情页-单位配置
   1. 模块名称变更为“适用企业”
   2. 列表字段名称：组织简称->企业简称，组织名称->企业名称，组织类型->企业类型，增加行业类型字段

### 安全投入计划

 	1. 安措计划、反措计划、其他计划的详情页
 	 	1. 项目编号字段可编辑
 	2. 安措计划、反措计划、其他计划的列表页
 	 	1. 在责任人字段右侧增加字段
 	     	1. 数据来源：人工录入、隐患治理、安全培训、应急演练

### 预算与使用

1. 安全预算详情页-详细预算

   1. 编辑状态下：

      1. 列表字段变更为
         1. 序号
         2. 项目名称
         3. 投入类别名称
         4. 两措类型：安措、反措、其他
         5. 计划投入费用（元）
         6. 投入预算（元）：数字输入框
      2. 在列表上方增加操作按钮
         1. 新增计划：打开弹窗新增计划
            1. 弹窗内一行一个字段
            2. 弹窗名称：新增计划
            3. 弹窗字段：
               1. 项目编号：必填，可编辑，默认为AQTRJH-20260101-0004
               2. 两措属性：安措、反措、其他
               3. 所属类别：必填，下拉框
               4. 项目名称：必填，文本框手动输入
               5. 项目内容：必填，文本域，字数限制0/200
               6. 计划完成时间：必填，日期选择器
               7. 计划投入费用（元）：必填
               8. 责任单位：必填，下拉选择器
               9. 责任人：必填，文本输入框
               10. 备注：必填，文本域，字数限制0/200
            4. 按钮
               1. 取消
               2. 保存

   2. 查看状态下：

      1. 列表字段变更为

         1. 序号
         2. 投入类别名称
         3. 计划投入费用（元）
         4. 投入预算（元）

      2. 列表使用嵌套表格的方式展示，如图所示

         ![image-20260108145554519](/Users/qijiayi/Library/Application Support/typora-user-images/image-20260108145554519.png)

      3. 嵌套的子表内字段为：

         1. 序号
         2. 项目名称
         3. 投入类别名称
         4. 两措类型：安措、反措、其他
         5. 计划投入费用（元）
         6. 投入预算（元）

2. 费用使用记录-详情页

   1. 基础信息
      1. 在“是否两措”字段左侧增加字段
         1. 字段名称：数据来源
         2. 具体要求：必填，单选组
         3. 选项为：计划内、计划外
            1. 选择计划内：现有的字段显示即可
            2. 选择计划外：项目名称字段的输入组件变更为文本输入框，所属类别、责任单位（部门）、责任人字段变更为可编辑状态，隐藏项目内容、计划完成时间、计划投入费用字段。
   2. 实际投入
      1. 在备注字段上方增加一个字段
         1. 字段名称：实际项目内容
         2. 具体要求：必填，文本域，独占一行，字数限制为0/200

### 安全费用计提

1. 详情页-详细计划
   1. 列表的本周期计划投入费用（万元）字段名称变更为“本周期计划投入预算（元）”





## 20260109优化内容

### 综合统计看板

1. **筛选区域
   1. 去掉“对比维度”
   2. 去掉“查询”、“重置”按钮
2. **核心指标卡片**
   1. 年度投入预算总额（元）卡片：去掉预算调整次数字段
   2. 增加**年度计提总额（元）卡片**：
      - 显示：总计提金额
      - 同比/环比变化
   3. 去掉投入执行率（%）卡片
3. **趋势分析图表区**
   1. 投入类别分布图：饼图增加每个扇形的标注
   2. 两措计划执行情况：名字改为“计划执行情况”
   3. 去掉“部门投入排名”，替换为关键指标预警表格
4. **关键指标预警**
   1. “涉及组织”字段名改为“涉及部门”
   2. 去掉“操作”列

### 多维度统计分析

1. 组织树
   1. 不要标题
   2. 不要多选
2. 查询条件区
   1. 参考安全投入其他的列表页面的查询条件区域的字段排布和按钮放置的规则
   2. 统计周期：年份选择器，默认为当前年
   3. 时间粒度：改为下拉选择框，默认选择年份
   4. 投入类别、金额范围、执行率范围：去掉
3. 快捷统计卡片区
   1. 和综合统计看板页面的4个卡片保持一致
4. 数据展示区
   1. 去掉当前的tab分类，按照以下需求调整显示的内容
   2. Tab 1:类别维度
      1. 表格字段：
         - 序号
         - 投入类别名称
         - 计划投入费用（万元）
         - 投入预算费用（万元）
         - 计提费用（万元）
         - 实际投入费用总额（万元）
         - 如果查询条件区的时间粒度选择季度，增加列字段
           - 一季度
           - 二季度
           - 三季度
           - 四季度
         - 如果查询条件区的时间粒度选择月份，增加列字段
           - 1月-12月一个月一列
      2. 表格的第一行为固定的合计行
   3. Tab 2:项目维度
      1. 表格字段：
         1. 序号
         2. 项目名称
         3. 投入类别名称
         4. 两措类型
         5. 计划投入费用（万元）
         6. 投入预算费用（万元）
         7. 计提费用（万元）
         8. 实际投入费用总额（万元）
         9. 责任部门



## 20260112优化内容

### 1. 数据看板

- 去掉数据范围和组织范围筛选条件
- 去掉每一个卡片的同比、环比数据
- 把卡片内金额数字后的“万”字去掉，放到卡片名称的（元）字一起，展示为（万元）
- 将“年度计提总额”和“年度实际投入总额”卡片合为一个卡片，中间用分割线隔开，“完成率”改为“投入率”，并且在文字右侧增加一个提示图标，鼠标移入后气泡显示“投入率=年度实际投入总额/年度计提总额*100%”
- 月度投入趋势图去掉“计划投入”、“投入预算”、“计提金额”值
- 隐藏“计划执行情况”和“关键指标预警”的卡片

### 2. 安全预算

- 详情页详细计划去掉合计行，放到列表的右上角，文字标识：计划投入费用总额（元）：xxx｜投入预算总额（元）：xxx
- “xxx”为2位小数的数字，千分号间隔，数字加粗
- 编辑页和详情查看页的合计行同步按照上述要求修改

### 3. 统计分析

- 左侧组织树承包商下级增加项目
- 项目维度tab页内的列表，去掉“计提费用（万元）”字段

### 4. 安全投入（相关方）

- 安全投入计划菜单打开的页面需要调整
  - 列表页
    - 左侧增加项目树，根节点为相关方名称，下面挂的是各个项目名称
    - 增加“关联相关方项目”字段，放在“责任单位（部门）”字段左侧
  - 详情页
    - 增加“关联相关方项目”字段，为必填项，单选下拉框，下拉选项为相关方下的各个项目名称，也支持点击放大镜打开弹窗选择项目，放在项目编号前面
    - 弹窗内支持根据相关方项目的项目名称、项目编号进行搜索选择
  - 以上的调整只针对安全投入（相关方）的安全投入计划菜单，安全投入（内部企业）的页面不受影响



## 20260113优化内容

### 1. 菜单结构优化

- 安全生产费用管理（内部企业）
  - 安全费用标准库
    - 计提标准库
    - 支出范围库
    - 负面清单库
  - 数据看板
  - 安全费用计划
  - 安全费用计提
  - 安全费用使用
  - 统计查询
- 安全生产费用管理（相关方）
  - 安全费用计划
  - 安全费用使用
- 根据上述描述调整安全投入主页的对应菜单结构，负面清单库先建立菜单，具体页面稍后会给具体的需求



### 2. 安全费用标准库

- 计提标准库
  - 左侧树
    - 搜索框的提示词改为：“搜索企业分类”
    - 根节点名称为：“安全生产费用企业分类”
  - 列表
    - “所属行业类型”字段名改为“所属企业分类”
  - 详情
    - 基本信息
      - “所属行业类型”字段名改为“所属企业分类”
    - 投入范围
      - 模块名改为“支出范围”
      - 提示文本改为“支出范围数据来源于支出范围库，根据“所属企业类型”自动关联，仅供查看”
      - 列表字段
        - 投入类别名称改为“支出范围名称”
        - 去掉“两措属性”
- 支出范围库
  - 左侧树：和计提标准库保持一致
  - 操作按钮区增加导入按钮
  - 查询区域：
    - 投入类别名称改为：支出范围名称
  - 列表
    - 投入类别名称改为：支出范围名称
    - 不需要子级，仅一级
    - 安全投入行业分类名称改为：所属企业分类
    - 去掉“两措属性”列
  - 新建弹窗
    - 投入类别名称改为：支出范围名称
    - 安全投入行业分类名称改为：所属企业分类
    - 不需要上级投入类别和两措属性字段
- 负面清单库
  - 左侧树：和计提标准库保持一致
  - 功能按钮
    - 新建：打开弹窗进行新建
    - 导入
    - 批量删除
  - 查询区域
    - 所属支出范围名称：模糊搜索
    - 负面清单名称：模糊搜索
  - 列表字段
    - 所属企业类型
    - 所属支出范围名称
    - 负面清单名称
  - 新建弹窗
    - 字段和列表字段一致，一个字段占一行

### 2.安全费用计划

- 列表
  - 查询区域
    - 投入类别改为“支出范围”
  - 表格
    - 字段
      - 投入类别改为“支出范围”
      - 去掉“数据来源”列
    - 操作列
      - 增加变更按钮
        - 点击变更按钮进入编辑页，可以针对字段进行编辑保存，保存后会在历史记录中插入变更前的数据记录
- 详情
  - 投入类别改为“支出范围”
  - 在查看状态时，顶部增加tab页，tab标签为：当前计划/历史记录
    - 当前计划：最新变更后的计划内容
    - 历史记录：通过列表的方式记录历史版本的该项目计划数据，点击某一版本可以查看此版本的计划内容

### 3. 安全费用计提

- 列表
  - 投入预算（元）字段变更为：“提取金额（元）”
  - 操作列增加图标按钮
    - 补提费用：点击补提费用打开详情页进行费用补提
- 详情页
  - “本周期计划投入预算（元）”变更为“本周期计划金额（元）”
  - “本周期计提金额（元）”变更为“本周期提取金额（元）”
- 补提费用详情页
  - 基本信息：和计提详情页字段一致
  - 提取数据：在计提详情页的基础上增加和修改
    - 本周期提取金额（元）名称变更为：“本周期已提取金额（元）”，不可编辑，仅查看
    - 增加：本周期补提金额（元），必填，两位小数，千分号分隔
    - 增加：本周期累计提取金额（元），必填，两位小数，千分号分隔，并且增加提示图标移入气泡提示：本周期累计提取金额=本周期已提取金额+本周期补提金额
  - 详细计划：在计提详情页的列表基础上变更：
    - 本周期提取金额（元）名称变更为：“本周期已提取金额（元）”，不可编辑，黑色文本
    - 右侧增加一列为：本周期补提金额（元）
- 费用补提后，需要记录下每次补提的记录，支持查看



### 4. 安全费用使用

- 所属类别改为“支出范围”
- 去掉“是否两措”字段
- 计划投入费用（元）改为“计划金额（元）”
- 实际投入费用（元）改为“实际金额（元）”
- 详情页
  - 去掉“数据来源”、“是否两措”字段



### 5. 数据看板

- 卡片区
  - 年度计划投入总额（万元）变更为“计划总额（万元）”
  - 去掉“年度投入预算总额（万元）”卡片
  - 年度计提总额（万元）变更为“提取总额（万元）”
  - 年度实际投入总额（万元）变更为“实际总额（万元）”
  - 投入率的提示图标内的气泡文本需要同步变更
  - 去掉一个卡片后需要合理布局，计划总额占1/3，剩下的卡片占2/3
- 图表区
  - 月度投入趋势
    - 标题名变更为“月度趋势图”
    - 增加提取金额，以折线的方式展示，每个月的提取金额为“提取总额（万元）”/12为每个月的提取金额
  - 投入类别分布图
    - 标题名变更为“支出范围实际分布图”
    - 预置的展示数据的支出范围参看支出范围库内置的电力生产与供应企业的支出范围



### 6. 统计查询（多维度统计分析）

- 卡片区域和数据看板的卡片保持一致
- “类别维度”名称变更为“支出范围维度”
- “投入类别名称”变更为“支出范围名称”
- “计划投入费用”变更为“计划总额（万元）”
- 去掉“投入预算费用（万元）”列
- “计提费用（万元）”变更为“提取总额（万元）”
- “实际投入费用总额（万元）”变更为“实际总额（万元）”



## 20260113-02优化内容

### 1.安全费用计划

- 新增一个新的列表页
  - tab页签
    - 报子公司计划
      - 不需要按钮
      - 列表字段
        - 年度：倒序排序
        - 年度计划总额（元）：两位小数，千分号间隔
        - 状态：未提交、审批中、审批通过、已驳回
      - 操作列
        - 查看
        - 提交：点击确认提交至子公司审批
    - 实际安全费用计划（默认选中）
      - 列表字段
        - 年度
        - 年度计划总额（元）
        - 计划项目数：蓝色超链接文本，点击进入下一级页面
        - 操作列
          - 查看：进入下一级页面
          - 新增计划：打开 @安措计划-详情.html 维护数据，关闭后需要进入下一级页面
      - 下一级页面为 @安措计划.html 

### 2.安全费用提取

- 提取数据模块
  - 提取依据名称字段去掉
  - “提取依据值”字段名称根据提取依据名称的字段内容进行显示
- 列表页
  - 在“提取金额（元）”右侧增加“补提记录”，为数字，蓝色超链接文本，点击跳转到详情页的“补提记录”tab页内





## 20260114 待补充

### 1. 提醒

- 安全费用计划
  - 增加提醒弹窗，按钮名称：“提醒设置”
    - 提醒编制预审计划：每年x月x日，提醒xxx、xxx
    - 提醒提交预审计划：每年x月x日，提醒xxx、xxx
    - 提醒维护正式计划：每年x月x日，提醒xxx、xxx
- 安全费用提取
  - 增加提醒弹窗，按钮名称：“提醒设置”
    - 提醒提取费用：年度x月x日，提醒xxx、xxx或者月度x日，提醒xxx、xxx
    - 提醒补提（本周期使用总额超过当前提取金额）：提醒xxx、xxx
- 安全费用使用
  - 增加提醒弹窗，按钮名称：“提醒设置”
    - 提醒提而不用：年度x月x日，提醒xxx、xxx或者月度x日，提醒xxx、xxx
    - 提醒使用率不足：年度x月x日，提醒xxx、xxx或者月度x日，提醒xxx、xxx

### 2. 负面清单识别

- 项目名称和项目内容录入负面清单的内容时进行提示
- 在安措计划的新建和变更弹窗内，输入项目名称和项目内容时需要对这两个字段输入的内容进行负面清单的匹配。前端预置一个数据的效果
  - 当输入为“环保用防控粉尘等设施设备的支出”时，在输入组件下方红色文字提示：“疑似负面清单，不合规请检查。”

### 3. 安全费用提取

- 增加月度实时提取和项目进度提取的页面和字段内容
- 增加方式：点击新建按钮的时候增加一个预选弹窗，有三种类型：月度实时提取、年度分摊提取、工程进度提取，选择进入对应的详情页面
- 年度分摊提取：当前的详情页
- 月度实时提取：
  - 基础数据：字段不变，默认值进行变更
    - 组织名称：xx矿山公司
    - 所属企业类型：非煤矿山开采企业
    - 提取类型：金属矿山（露天矿山）
    - 提取规则：比例提取
    - 提取周期类型：月度实时提取
    - 提取周期：月份选择框

  - 提取数据
    - 字段名称显示为：原矿产量（吨）
    - 本周期应提取金额（元）：为原矿产量（吨）*比例（每吨5元），计算规则弹窗内对应调整
    - 本周期计划金额（元）：不变
    - 上一周期结余金额（元）：不变
    - 本年度累计提取金额（元）：新增，必填，不可编辑，2位小数，千分号分隔
    - 本年度累计支出金额（元）：新增，必填，不可编辑，2位小数，千分号分隔
    - 本周期提取金额（元）：不变

  - 详细计划：列表不变
    - 增加：在本周期计划金额（元）左侧增加“本年度累计提取金额（元）”和“本年度累计支出金额”列，仅查看，不可编辑

- 工程进度提取：
  - 基础数据：字段不变，默认值进行变更
    - 组织名称：xx工程
    - 所属企业类型：建设工程施工企业
    - 提取类型：水利水电工程
    - 提取规则：比例提取
    - 提取周期类型：工程进度提取
    - 提取周期：月份选择框
  - 提取数据
    - 字段名称显示为：建筑安装工程造价（元）
    - 增加：当前工程进度（%）：
    - 增加：本周期工程进度（%）：
    - 本周期应提取金额（元）：为本周期工程进度（%）* 建筑安装工程造价（元） * 比例（2.5%），计算规则弹窗内对应调整
    - 本周期计划金额（元）：不变
    - 上一周期结余金额（元）：不变
    - 本年度累计提取金额（元）：新增，必填，不可编辑，2位小数，千分号分隔
    - 本年度累计支出金额（元）：新增，必填，不可编辑，2位小数，千分号分隔
    - 本周期提取金额（元）：不变
  - 详细计划：列表不变
    - 增加：在本周期计划金额（元）左侧增加“工程累计提取金额（元）”和“工程累计支出金额”列，仅查看，不可编辑


## 20260114现场评审优化内容

### 1. 菜单调整

- 安全费用标准库
  - 计提标准库
  - 支出范围库
  - 负面清单库
- 数据看板
- 安全费用计划
  - 预审计划：跳转页面暂时先不添加
  - 正式计划：跳转页面暂时先不添加
- 安全费用提取
  - 计划提取：安全费用计提.html
  - 实际提取：跳转页面暂时先不添加
- 安全费用使用
- 统计查询



### 2. 预审计划

- 列表页

  - 年度
  - 企业名称
  - 年度计划总额（元）
  - 状态：草稿、审批中、已上报
  - 操作列
    - 查看
    - 编辑

- 详情页

  - 本企业计划

    - 列表展示各个项目计划内容
      - 功能按钮
        - 新建
        - 批量删除
        - 导入
        - 导出
      - 查询区域
        - 和 @安措计划.html 页面保持一致
      - 列表字段
        - 和 @安措计划.html 页面保持一致
      - 操作按钮
        - 取消
        - 保存
        - 提交审批

  - 下级企业计划

    - 列表展示各下级企业的计划总值（点击项目数可以查看详细项目计划表）

      - 列表字段
        - 年度
        - 企业名称
        - 年度计划总额（元）
      - 操作按钮
        - 查看：点击查看计划详情
      - 一个企业一条数据，预置2-3个企业

    - 计划详情页面和预审计划的详情页展示的内容一样，但是需要增加tab页签，分别为：项目维度、类别维度

      - 项目维度：为列表，展示各个项目数据

      - 类别维度：列表，字段展示各类别的计划金额

        - 序号
        - 支出范围名称：根据 @99示例.html 的数据预置

        - 计划总额（元）



### 3. 正式计划

- 基于当前页面进行调整
- 左侧为卡片列表
  - 卡片标题：年度
  - 卡片字段：计划总额（元）、项目数量
- 右侧为点击卡片展示详细的项目计划
  - 在项目编号左侧增加一列序号

 ### 4. 计划变更

- 在“正式计划”下增加一个同级菜单名为“计划变更”
- 功能按钮
  - 新建
  - 批量删除
- 查询区域
- 列表
  - 字段
    - 编号
    - 变更计划年份
    - 变更状态
    - 变更人
    - 变更时间
  - 操作按钮
    - 查看：打开详情页面
    - 编辑
    - 删除
- 详情页
  - 分左右两块，左边为变更前，右边为变更后
  - 具体内容均为项目计划列表
  - 需要标注出存在区别的内容

### 5. 计划提取

#### 年度分摊提取类型

- 调整@计划提取-详情.html 页面

- 左侧为卡片列表
  - 卡片标题：年度
  - 卡片字段：本年度应提取金额（元）、上一年度结余金额（元）、本年度可使用金额（元）
- 右侧为点击卡片展示详情
  - 基础数据：和 @计划提取-详情.html 页面的字段一致
  - 计提数据：和 @计划提取-详情.html 页面的字段一致
  - 月度详细计提
    - 列表形式展示
      - 月份：1-12月，1月一行
      - 计划提取金额（元）：可编辑输入金额，2位小数，千分号分隔

#### 月度实时提取

- 调整@计划提取-详情-月度.html 
- 左侧为卡片列表：同年度分摊提取类型
- 右侧为点击卡片展示详情：
  - 基础数据：和 @计划提取-详情-月度.html 字段一致
  - 计提数据：和 @计划提取-详情-月度.html 字段一致
  - 月度详细计提
    - 列表形式展示
      - 月份：1-12月，1月一行
      - 计划提取金额（元）：可编辑输入金额，2位小数，千分号分隔

#### 工程进度提取





### 年度分摊提取

- 卡片
  - 卡片标题：年度
  - 卡片字段：2026年应提取金额(元)、2025年结余金额(元)、2026年可使用金额(元)、2026年实际提取金额(元)
- 列表
  - 基础数据
    - 去掉提取周期字段
  - 计提数据->提取数据
    - 上一年度营业收入（元）：不变
    - 本年度应提取金额（元）->2026年应提取金额（元）
    - 上一周期结余金额（元）->2025年结余金额（元）
    - 本周期提取金额（元）->2026年可使用金额（元）：改为不可编辑
    - 增加：2026年补提金额（元），不可编辑，并增加图标提示：“企业安全生产费用出现赤字（即当年计提企业安全生产费用加上年初结余小于年度实际支出）的，应当于年末补提企业安全生产费用。如果补提金额数字为0，则代表当前未发生补提。”
  - 月度详细计提->月度提取数据
    - 计划提取金额（元）->应提取金额
    - 增加实际提取金额（元）列，同应提取金额，为手动输入，默认均为空

### 月度实时提取

- 卡片
  - 卡片标题：年度
  - 卡片字段：2026年应提取金额(元)、2025年结余金额(元)、2026年可使用金额(元)、2026年实际提取金额(元)
- 列表
  - 基础数据
    - 去掉提取周期字段
  - 计提数据->提取数据
    - 2025年结余金额（元）
    - 2026年累计提取金额（元）
    - 2026年可使用金额（元）：为2025年结余金额+2026年累计提取金额，需要增加图标提示计算规则
    - 2026年补提金额（元），不可编辑，并增加图标提示：“企业安全生产费用出现赤字（即当年计提企业安全生产费用加上年初结余小于年度实际支出）的，应当于年末补提企业安全生产费用。如果补提金额数字为0，则代表当前未发生补提。”
  - 月度详细计提->月度提取数据
    - 月份
    - 计划提取金额（元）->原矿产量（吨）
    - 增加：应提取金额（元），不可编辑，自动汇算
    - 增加：实际提取金额（元），同原矿产量（吨），为手动输入，默认和应提取金额（元）一致

### 项目进度提取

- 不需要卡片，直接打开同@计划提取-详情-工程进度.html，并调整具体内容
  - 基础数据：去掉提取周期字段
  - 提取数据：只保留建筑安装工程造价（元）、本周期应提取金额（元）、本周期实际提取金额（元）
    - 同时需要处理：本周期应提取金额（元）->工程应提取金额（元）、本周期实际提取金额（元）->工程实际提取金额（元）
  - 去掉详细计划







### 安全费用使用

- 左侧增加卡片
  - 卡片标题：年度
  - 卡片字段：可使用金额（元）、实际提取总额（元）、已使用金额（元）
- 右侧列：
  - 增加tab页：计划使用情况、使用详细记录
    - 计划使用情况：
      - 列表已有字段不动，在最后增加列，为1-12月，1个月1列，数值为这条计划在那个月的实际支出金额，如果没有则为空
      - 列表左右移动时，固定在项目名称列不动
    - 使用详细记录
      - 功能按钮：和计划使用情况一样
      - 搜索区域：项目名称、实际使用内容、实际使用时间
      - 字段：编号、项目名称、实际使用内容、支出范围类别、实际使用时间、实际使用金额（元）、责任部门、责任人
      - 操作列按钮：查看、编辑、删除

- 详情
  - 增加查看详情页，点击超链接文本和查看按钮打开查看详情页
  - 查看详情页基于现在的详情页，将实际投入的内容变更为列表的形式展示，字段不变



### 预审计划

- 左侧增加卡片
  - 卡片标题：年度
  - 卡片字段：年度计划总额（元）
- 右侧
  - 现在的表单不变
  - 年度计划总额（元）：左对齐、超链接文本，点击打开详情页
- 查看和超链接文本打开的详情页
  - 去掉“汇总数据”和“下级企业计划”模块
  - “本企业计划”
    - 增加tab页：支出范围维度（默认选择）、项目维度
    - 项目维度：现有内容不变
    - 支出范围维度：
      - 列表字段：
        - 序号
        - 支出范围名称
        - 年度计划总额（元）



### 20260121优化内容

#### 1、支出范围库

- 列表
  - 支出范围名称右侧增加“支出范围简称”列，在新建和编辑弹窗中也要对应增加该字段



支持范围类别->支出范围简称









