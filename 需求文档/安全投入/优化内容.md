## 安全费用标准库列表

1. 行业分类树去掉顶部的行业分类标题
2. 新建按钮行的右侧不需要显示当前选中的内容
3. 查询、重置按钮默认居右放置
4. 编码不需要蓝色字体和点击跳转的交互，应当设置给提取类型字段，并且超链接文本默认为蓝色下划线的文字，点击后跳转到详情页
5. 提取周期类型和提取规则类型不要用标签的样式，用文字展示即可
6. 操作列按钮为纯图标按钮，不需要边框底色，不需要按钮文字

## 安全费用标准库详情页

1. 不要顶部的页面名称行
2. 详细提取规则行的三个字段均为必填项，增加必填项的*
3. 投入范围和单位配置的表格上方不需要显示投入类别列表和关联组织列表文字
4. 投入范围和单位配置的添加、删除按钮居左显示

## 公司投入预算列表页

1. 不要操作按钮区上方的公司投入预算标题行
2. 不需要显示操作按钮区右侧的共x条记录的文字
3. 查询、重置按钮默认居右放置
4. 编号不需要蓝色字体和点击跳转的交互，应当设置给投入预算（元）字段，并且超链接文本默认为蓝色下划线的文字，点击后跳转到详情页
5. 操作列按钮为纯图标按钮，不需要边框底色，不需要按钮文字

## 公司投入预算详情页

1. 不要顶部的页面名称行
2. 提取依据值（元）、应提取金额（元）、上一周期结余金额（元）、投入预算（元）均为必填项，增加必填项的*
3. 提取依据值（元）、应提取金额（元）、投入预算（元）字段下方去掉小字提示
4. 应提取金额（元）用文本输入框的方式展示数据，为不可编辑状态
5. 投入预算（元）用文本输入框的方式展示数据，可编辑

## 数据看板

1. 同比和环比的数值看起来非常的不显眼，尤其是和卡片背景为同样的色调时，请调整设计
2. 年份选择器放到2026年度安全投入标题文字的右侧
3. 调整数据看板的页面，希望可以一页放下，不需要滚动查看

## 统计分析

1. 部门树不需要标题
2. 查询区域不需要显示当前选择的部门节点
3. 卡片区域小一些，合理调整一下高度



## 安全费用计提管理详情页

1. 提取依据值字段的输入组件右侧不需要“万元”字样
2. 本周期应提取金额（万元）的输入组件右侧增加一个外键图标按钮，点击图标按钮可以打开弹窗查看计算规则，将计算规则以可视查看的方式显示出来
3. 本页面内关于金额的字段或者列表内的数值，如果是小数点末尾的0请不显示便于查看数据
4. 本页面内每个字段的字段名称请设置最多显示6个字，如果超长了请换行显示
5. 本周期计提金额（万元）字段改为可以编辑
6. 详细计划的合计以表格行的形式展示在最下面一行

## 20260108-001优化内容

### 计提标准库

1. 详情页-单位配置
   1. 模块名称变更为“适用企业”
   2. 列表字段名称：组织简称->企业简称，组织名称->企业名称，组织类型->企业类型，增加行业类型字段

### 安全投入计划

 	1. 安措计划、反措计划、其他计划的详情页
 	 	1. 项目编号字段可编辑
 	2. 安措计划、反措计划、其他计划的列表页
 	 	1. 在责任人字段右侧增加字段
 	     	1. 数据来源：人工录入、隐患治理、安全培训、应急演练

### 预算与使用

1. 安全预算详情页-详细预算

   1. 编辑状态下：

      1. 列表字段变更为
         1. 序号
         2. 项目名称
         3. 投入类别名称
         4. 两措类型：安措、反措、其他
         5. 计划投入费用（元）
         6. 投入预算（元）：数字输入框
      2. 在列表上方增加操作按钮
         1. 新增计划：打开弹窗新增计划
            1. 弹窗内一行一个字段
            2. 弹窗名称：新增计划
            3. 弹窗字段：
               1. 项目编号：必填，可编辑，默认为AQTRJH-20260101-0004
               2. 两措属性：安措、反措、其他
               3. 所属类别：必填，下拉框
               4. 项目名称：必填，文本框手动输入
               5. 项目内容：必填，文本域，字数限制0/200
               6. 计划完成时间：必填，日期选择器
               7. 计划投入费用（元）：必填
               8. 责任单位：必填，下拉选择器
               9. 责任人：必填，文本输入框
               10. 备注：必填，文本域，字数限制0/200
            4. 按钮
               1. 取消
               2. 保存

   2. 查看状态下：

      1. 列表字段变更为

         1. 序号
         2. 投入类别名称
         3. 计划投入费用（元）
         4. 投入预算（元）

      2. 列表使用嵌套表格的方式展示，如图所示

         ![image-20260108145554519](/Users/qijiayi/Library/Application Support/typora-user-images/image-20260108145554519.png)

      3. 嵌套的子表内字段为：

         1. 序号
         2. 项目名称
         3. 投入类别名称
         4. 两措类型：安措、反措、其他
         5. 计划投入费用（元）
         6. 投入预算（元）

2. 费用使用记录-详情页

   1. 基础信息
      1. 在“是否两措”字段左侧增加字段
         1. 字段名称：数据来源
         2. 具体要求：必填，单选组
         3. 选项为：计划内、计划外
            1. 选择计划内：现有的字段显示即可
            2. 选择计划外：项目名称字段的输入组件变更为文本输入框，所属类别、责任单位（部门）、责任人字段变更为可编辑状态，隐藏项目内容、计划完成时间、计划投入费用字段。
   2. 实际投入
      1. 在备注字段上方增加一个字段
         1. 字段名称：实际项目内容
         2. 具体要求：必填，文本域，独占一行，字数限制为0/200

### 安全费用计提

1. 详情页-详细计划
   1. 列表的本周期计划投入费用（万元）字段名称变更为“本周期计划投入预算（元）”





## 20260109优化内容

### 综合统计看板

1. **筛选区域
   1. 去掉“对比维度”
   2. 去掉“查询”、“重置”按钮
2. **核心指标卡片**
   1. 年度投入预算总额（元）卡片：去掉预算调整次数字段
   2. 增加**年度计提总额（元）卡片**：
      - 显示：总计提金额
      - 同比/环比变化
   3. 去掉投入执行率（%）卡片
3. **趋势分析图表区**
   1. 投入类别分布图：饼图增加每个扇形的标注
   2. 两措计划执行情况：名字改为“计划执行情况”
   3. 去掉“部门投入排名”，替换为关键指标预警表格
4. **关键指标预警**
   1. “涉及组织”字段名改为“涉及部门”
   2. 去掉“操作”列

### 多维度统计分析

1. 组织树
   1. 不要标题
   2. 不要多选
2. 查询条件区
   1. 参考安全投入其他的列表页面的查询条件区域的字段排布和按钮放置的规则
   2. 统计周期：年份选择器，默认为当前年
   3. 时间粒度：改为下拉选择框，默认选择年份
   4. 投入类别、金额范围、执行率范围：去掉
3. 快捷统计卡片区
   1. 和综合统计看板页面的4个卡片保持一致
4. 数据展示区
   1. 去掉当前的tab分类，按照以下需求调整显示的内容
   2. Tab 1:类别维度
      1. 表格字段：
         - 序号
         - 投入类别名称
         - 计划投入费用（万元）
         - 投入预算费用（万元）
         - 计提费用（万元）
         - 实际投入费用总额（万元）
         - 如果查询条件区的时间粒度选择季度，增加列字段
           - 一季度
           - 二季度
           - 三季度
           - 四季度
         - 如果查询条件区的时间粒度选择月份，增加列字段
           - 1月-12月一个月一列
      2. 表格的第一行为固定的合计行
   3. Tab 2:项目维度
      1. 表格字段：
         1. 序号
         2. 项目名称
         3. 投入类别名称
         4. 两措类型
         5. 计划投入费用（万元）
         6. 投入预算费用（万元）
         7. 计提费用（万元）
         8. 实际投入费用总额（万元）
         9. 责任部门
